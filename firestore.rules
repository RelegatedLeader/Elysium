rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated users to read/write notes for debugging
    match /notes/{noteId} {
      allow read, write, create: if request.auth != null;
    }
  }
}